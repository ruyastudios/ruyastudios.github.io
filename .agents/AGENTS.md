# Project Rules

- **Update Counter**: Always increment the "UPDATE #X" counter in `index.html` whenever pushing an update to the repository. This is an implicit rule for every modification to the project.
